#ifndef MAIN_H
#define MAIN_H

#include <string>
#include <vector>

using namespace std;

class unit
{
	int id;
	int type;
};

class player
{
	string name;
	vector<unit*> onBoard;
	vector<unit*> inHand;
};

player::player()
{
	unit playerUnit[20];
	unit playerUnit[0].id = 0;
	unit playerUnit[1].id = 1;
	unit playerUnit[2].id = 1;
	unit playerUnit[3].id = 2;
	unit playerUnit[4].id = 2;
	unit playerUnit[5].id = 3;
	unit player
}

// FUNCTIONS
int init()
{
}

#endif
